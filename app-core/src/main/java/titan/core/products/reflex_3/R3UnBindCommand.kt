package titan.core.products.reflex_3

import titan.core.bluetooth.CommManager
import titan.core.products.TaskPackage

/**
 * Created by Sai Vinay Mohan I.
 * Titan Company Ltd
 */
/*
fun codeBinding() : R3BindCommand{
    CommManager.getInstance().executeCommand {
        TaskPackage(
            write = Pair(
                R3_COMMUNICATION_SERVICE,
                R3_COMMUNICATION_WRITE_CHAR
            ),
            read = null,
            responseWillNotify = true,
            data = byteArrayOf(commandId, 3, 2, 10, 1, 8, 1, 2, 3, 4, 5, 6, 7, 8),
            key = null
            // TODO: Replace last 9 digits with random generated once.
        )
    }
    return this
}

fun unbind() {
    CommManager.getInstance().executeCommand {
        TaskPackage(
            write = Pair(
                R3_COMMUNICATION_SERVICE,
                R3_COMMUNICATION_WRITE_CHAR
            ),
            read = null,
            responseWillNotify = true,
            data = byteArrayOf(commandId, 2, 85, -86, 85, -86),
            key = null
        )
    }

}

fun cancelBinding() {
    CommManager.getInstance().executeCommand {
        TaskPackage(
            write = Pair(
                R3_COMMUNICATION_SERVICE,
                R3_COMMUNICATION_WRITE_CHAR
            ),
            read = null,
            responseWillNotify = true,
            data = byteArrayOf(commandId, 4),
            key = null
        )
    }
}*/
